import { Stack } from "expo-router";

const Layout = () => {
    return(
        <Stack screenOptions={{
            headerTitle:"Location",
            headerStyle:{
                backgroundColor:"0023C4"
            }
        }}/> 
    )
}

export default Layout